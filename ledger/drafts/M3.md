# M3 — Altitude undefined (Seam vs Fabric)

## Risk
Statement: Altitude undefined (Seam vs Fabric). Audience, language, and artifacts fork unresolvably.
Category: Operational
Gate: Define
Area: Definition (meta)

## Assumption(s)
### A1
Statement: We believe that three people independently tagging the same six work products as Seam or Fabric (or "cannot assign") will produce the same tag on at least five of six items when given only the altitude names and a one-line definition each — no live facilitation.
Success criteria: ≥5 of 6 items have unanimous Seam/Fabric tags across the three taggers; zero items tagged "cannot assign" by two or more taggers.
Failure criteria: 2 or more items disagree (not unanimous) or are "cannot assign" for two or more taggers.

## Slice(s)
### S1 (tests A1)
Method: Print six existing artifacts from this engagement only (unknown if a written Seam vs Fabric spec exists beyond the register wording — do not invent one). Candidates if they exist in-repo: `data/register.csv`, client `index.html` board, `internal/index.html`, `AGENT_BRIEF.md`, `AGENT_BRIEF_LEDGER.md`, this ledger draft folder. Each tagger gets the same one-line prompts: Seam = engagement-facing method/surface; Fabric = reusable firm system. They tag each artifact Seam / Fabric / cannot assign, silently, in one pass.
Type: Operational
Execution: Requires manual run
Scope / time-box: n=3 taggers; 6 artifacts; 20 minutes; tags discarded unless a later promotion run cites them.

## Signal
Success signal: Unanimous tag on ≥5 of 6 artifacts; ≤1 "cannot assign" from any single tagger.
Failure signal: ≥2 artifacts not unanimous, or ≥2 artifacts with two or more "cannot assign."
Threshold: Failure → Iterate: write a ratified altitude rule before more dual surfaces are committed. Success → altitude is operationally assignable enough that forking is not the default. Do not treat a workshop debate as signal.
Result: _pending — not yet run_

## Confidence & Decision
Current confidence: _pending signal_
Suggested read: Agent suggestion — not a decision: Low / undefined; High collapse on the register means wrong altitude is expensive if wrong — S1 is cheap and should run before more client/internal surfaces diverge.
Decision: _awaiting human review_

## Status
`draft`
